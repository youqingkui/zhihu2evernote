// Generated by CoffeeScript 1.8.0
(function() {
  var Evernote, async, changeImgs, cheerio, composeCreateNote, cookie, crypto, fs, getPageCount, getTag, makeNote, mime, nodeUrl, noteStore, op, pageImport, querystring, readImgRes, reqOp, request, rmAttr;

  request = require('request');

  async = require('async');

  cookie = process.env.ZhiHu;

  cheerio = require('cheerio');

  makeNote = require('./server/createNote');

  noteStore = require('./server/noteStore');

  Evernote = require('evernote').Evernote;

  fs = require('fs');

  crypto = require('crypto');

  mime = require('mime');

  nodeUrl = require('url');

  querystring = require('querystring');

  reqOp = function(url) {
    var options;
    options = {
      url: url,
      headers: {
        'User-Agent': 'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_10_3) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/42.0.2311.90 Safari/537.36',
        'Cookie': cookie
      }
    };
    return options;
  };

  pageImport = function(op, cb) {
    return async.auto({
      getPage: function(c) {
        return request.get(op, function(err, res, body) {
          var $, answerList;
          console.log(err);
          if (err) {
            return c(err);
          }
          $ = cheerio.load(body);
          answerList = $("#zh-list-answer-wrap > div");
          return c(null, answerList, $);
        });
      },
      getContent: [
        'getPage', function(c, result) {
          var $, answerList, noteArr, oldSourceUrl, oldTitle;
          answerList = result.getPage[0];
          $ = result.getPage[1];
          noteArr = [];
          oldTitle = '';
          oldSourceUrl = '';
          return async.eachSeries(answerList, function(item, callback) {
            var $2, content1, sourceUrl, tagFind, tagUrl, title;
            title = $(item).find("h2.zm-item-title").text();
            if (!title) {
              title = oldTitle;
            }
            oldTitle = title;
            sourceUrl = $(item).find("h2.zm-item-title a").attr('href');
            if (!sourceUrl) {
              sourceUrl = oldSourceUrl;
            } else {
              sourceUrl = 'http://www.zhihu.com' + sourceUrl;
            }
            oldSourceUrl = sourceUrl;
            content1 = $(item).find(".content.hidden").text();
            tagFind = $(item).find("a.toggle-expand");
            if (!tagFind.length) {
              tagUrl = sourceUrl;
            } else {
              tagUrl = 'http://www.zhihu.com' + $(tagFind).attr('href');
            }
            $2 = cheerio.load(content1, {
              decodeEntities: false
            });
            rmAttr($2);
            console.log("$2.html({xmlMode:true}),", $2.html({
              xmlMode: true
            }));
            return composeCreateNote($2, title, tagUrl, tagUrl, noteStore, function(err, note) {
              if (err) {
                return callback(err);
              }
              return callback();
            });
          }, function(eachErr) {
            if (eachErr) {
              return cb(eachErr);
            }
            return c(null, noteArr);
          });
        }
      ]
    }, function(eachErr) {
      console.log(eachErr);
      if (eachErr) {
        return cb(eachErr);
      }
      return cb();
    });
  };

  composeCreateNote = function($, title, tagUrl, sourceUrl, noteStore, cb) {
    return async.parallel([
      function(callback) {
        return getTag(tagUrl, function(tagList) {
          return callback(null, tagList);
        });
      }, function(callback) {
        return changeImgs($, $("img"), function(err, resourceArr) {
          var content;
          if (err) {
            return callback(err);
          }
          content = $.html({
            xmlMode: true
          });
          return callback(null, content, resourceArr);
        });
      }
    ], function(endErr, result) {
      var content, resourceArr, tagList;
      if (endErr) {
        return cb(endErr);
      }
      console.log("++++++++++++++++++++++++");
      console.log(result);
      console.log("++++++++++++++++++++++++");
      tagList = result[0];
      content = result[1][0];
      resourceArr = result[1][1];
      return makeNote(noteStore, title, tagList, content, sourceUrl, resourceArr, function(err, note) {
        if (err) {
          return cb(err);
        }
        console.log("create ok " + note.title);
        return cb();
      });
    });
  };

  getTag = function(url, cb) {
    var op;
    op = reqOp(url);
    console.log("tag url ==>", op);
    return request.get(op, function(err, res, body) {
      var $, tagArr, tagList;
      if (err) {
        console.log(err);
        return cb(null, []);
      }
      $ = cheerio.load(body);
      tagArr = $("a.zm-item-tag");
      tagList = [];
      tagArr.each(function(i, elem) {
        var tagName;
        tagName = $(elem).text().trim();
        return tagList.push(tagName);
      });
      console.log("tagListtagListtagListtagListtagList ====>", tagList);
      return cb(tagList);
    });
  };

  rmAttr = function($) {
    return $("a, span, img, i, div, code").map(function(i, elem) {
      var k, _results;
      _results = [];
      for (k in elem.attribs) {
        if (k !== 'src') {
          _results.push($(this).removeAttr(k));
        } else {
          _results.push(void 0);
        }
      }
      return _results;
    });
  };

  changeImgs = function($, $imgs, cb) {
    var resourceArr;
    resourceArr = [];
    return async.eachSeries($imgs, function(item, callback) {
      var src;
      src = $(item).attr('src');
      return readImgRes(src, function(err, resource) {
        var hexHash, md5, newTag;
        if (err) {
          return cb(err);
        }
        resourceArr.push(resource);
        md5 = crypto.createHash('md5');
        md5.update(resource.image);
        hexHash = md5.digest('hex');
        newTag = "<en-media type=" + resource.mime + " hash=" + hexHash + " />";
        console.log(newTag);
        $(item).replaceWith(newTag);
        return callback();
      });
    }, function(eachErr) {
      if (eachErr) {
        return console.log(eachErr);
      }
      return cb(null, resourceArr);
    });
  };

  readImgRes = function(imgUrl, cb) {
    var op;
    op = reqOp(imgUrl);
    op.encoding = 'binary';
    return async.auto({
      readImg: function(callback) {
        return request.get(op, function(err, res, body) {
          var mimeType;
          if (err) {
            return cb(err);
          }
          mimeType = res.headers['content-type'];
          return callback(null, body, mimeType);
        });
      },
      enImg: [
        'readImg', function(callback, result) {
          var data, hash, image, mimeType, resource;
          mimeType = result.readImg[1];
          image = new Buffer(result.readImg[0], 'binary');
          hash = image.toString('base64');
          data = new Evernote.Data();
          data.size = image.length;
          data.bodyHash = hash;
          data.body = image;
          resource = new Evernote.Resource();
          resource.mime = mimeType;
          resource.data = data;
          resource.image = image;
          return cb(null, resource);
        }
      ]
    });
  };

  getPageCount = function(url, cb) {
    return request.get(url, function(err, res, body) {
      var $, pageCount, spanArr;
      if (err) {
        return cb(err);
      }
      $ = cheerio.load(body);
      spanArr = $(".zm-invite-pager span");
      pageCount = $(spanArr[spanArr.length - 2]).text();
      return cb(null, Number(pageCount));
    });
  };

  op = reqOp('http://www.zhihu.com/collection/19932288');

  async.auto({
    getPage: function(cb) {
      return getPageCount(op, function(err, count) {
        if (err) {
          return cb(err);
        }
        console.log("count", count);
        return cb(null, count);
      });
    },
    importPage: [
      'getPage', function(cb, result) {
        var pageArr, pageCount, parseUrl, query, stratPage, _i, _j, _results, _results1;
        pageCount = result.getPage;
        parseUrl = nodeUrl.parse(op.url);
        query = querystring.parse(parseUrl.query);
        stratPage = Number(query.page);
        if (stratPage) {
          pageArr = (function() {
            _results = [];
            for (var _i = stratPage; stratPage <= pageCount ? _i <= pageCount : _i >= pageCount; stratPage <= pageCount ? _i++ : _i--){ _results.push(_i); }
            return _results;
          }).apply(this);
        } else {
          pageArr = [0].concat((function() {
            _results1 = [];
            for (var _j = 2; 2 <= pageCount ? _j <= pageCount : _j >= pageCount; 2 <= pageCount ? _j++ : _j--){ _results1.push(_j); }
            return _results1;
          }).apply(this));
        }
        console.log("pageArr ==>", pageArr);
        return async.eachSeries(pageArr, function(item, callback) {
          var newUrl, op2;
          if (item !== 0) {
            newUrl = parseUrl.protocol + '//' + parseUrl.hostname + parseUrl.pathname + '?page=' + item;
            op2 = reqOp(newUrl);
          } else {
            op2 = op;
          }
          console.log("op2 ==>", op2);
          return pageImport(op2, function(err, result) {
            if (err) {
              return cb(err);
            }
            console.log("" + op2.url + " is import ok");
            return callback();
          });
        }, function(eachErr) {
          if (eachErr) {
            return console.log(eachErr);
          }
          return console.log("all page do !!");
        });
      }
    ]
  });

}).call(this);

//# sourceMappingURL=app.js.map
